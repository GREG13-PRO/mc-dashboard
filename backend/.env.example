PORT=3000
SESSION_SECRET=change-me-to-a-long-random-string
# Generate a bcrypt hash for your admin password, e.g.:
#   node -e "console.log(require('bcryptjs').hashSync('yourpassword', 10))"
ADMIN_PASSWORD_HASH=
# Absolute path where servers.json / auth session data live
DATA_DIR=./data
